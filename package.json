{
  "name": "ec2-instance-manager",
  "version": "1.0.0",
  "private": true,
  "description": "AWS EC2 Instance Manager - Full Stack Application",
  "workspaces": [
    "apps/*",
    "packages/*",
    "infrastructure"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "build": "npm run build --workspaces --if-present",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "build:infra": "npm run build --workspace=infrastructure",
    "test": "npm run test --workspaces --if-present",
    "test:web": "npm run test --workspace=apps/web",
    "test:api": "npm run test --workspace=apps/api",
    "test:infra": "npm run test --workspace=infrastructure",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:workspaces": "npm run format --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    "deploy:dev": "npm run deploy:dev --workspace=infrastructure",
    "deploy:prod": "npm run deploy:prod --workspace=infrastructure",
    "destroy:dev": "npm run destroy:dev --workspace=infrastructure",
    "destroy:prod": "npm run destroy:prod --workspace=infrastructure",
    "infra:validate": "npm run validate --workspace=infrastructure",
    "cleanup:wrong-account": "./scripts/cleanup-wrong-account.sh",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "husky": "^8.0.3",
    "prettier": "^3.6.2",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
